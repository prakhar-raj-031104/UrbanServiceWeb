{
  "name": "urbanservice",
  "version": "1.0.0",
  "private": true,
  "description": "UrbanService — on-demand home services platform (cooking, washing, cleaning)",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,magenta \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "dev:api": "npm --prefix backend run dev",
    "dev:web": "npm --prefix frontend run dev",
    "seed": "npm --prefix backend run seed",
    "db:push": "npm --prefix backend run db:push",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
